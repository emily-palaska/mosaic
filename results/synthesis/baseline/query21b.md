# Baseline Code Synthesis
Query `Use post-pruning on decision tree.`
## Synthesis Blocks
### meta-llama/Llama-3.2-3B
Generated by LLM
```python

def query(data, attribute, value):
    if data == []:
        return []
    if len(data) == 1:
        return data[0]
    if attribute == 'Age':
        if value == 'adult':
            return query(data, 'Gender','male')
        else:
            return query(data, 'Gender', 'female')
    if attribute == 'Gender':
        if value =='male':
            return query(data, 'Age', 'adult')
        else:
            return query(data, 'Age', 'child')
    if attribute == 'Age':
        if value == 'adult':
            return query(data, 'Gender','male')
        else:
            return query(data, 'Gender', 'female')
    if
```

## Code Concatenation
```python

def query(data, attribute, value):
    if data == []:
        return []
    if len(data) == 1:
        return data[0]
    if attribute == 'Age':
        if value == 'adult':
            return query(data, 'Gender','male')
        else:
            return query(data, 'Gender', 'female')
    if attribute == 'Gender':
        if value =='male':
            return query(data, 'Age', 'adult')
        else:
            return query(data, 'Age', 'child')
    if attribute == 'Age':
        if value == 'adult':
            return query(data, 'Gender','male')
        else:
            return query(data, 'Gender', 'female')
    if
```
